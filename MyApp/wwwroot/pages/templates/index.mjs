import JamstackTemplates from "../templates/JamstackTemplates.mjs"

export default {
    install(app) {
    },
    components: {
        JamstackTemplates,
    },
    setup() {
        return { }
    }
}
