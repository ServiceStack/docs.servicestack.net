import AudioPlayer from "../podcasts/AudioPlayer.mjs"
import AsciiCinema from '../components/AsciiCinema.mjs'

export default {
    install(app) {
    },
    components: {
        AudioPlayer,
        AsciiCinema,
    },
    setup() {
        return { }
    }
}
